<?xml version="1.0" encoding="utf-8"?>
<com.qdcares.qdcaresrecyclerview.swipemenu.SwipeItemLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipe_layout"
    android:layout_width="match_parent"
    android:layout_height="80dp">

    <TextView
        android:id="@+id/left_menu"
        android:layout_width="150dp"
        android:layout_height="match_parent"
        android:layout_gravity="left"
        android:background="@color/colorPrimary"
        android:gravity="center"
        android:text="left"
        android:textColor="#ffffff" />

    <TextView
        android:id="@+id/right_menu"
        android:layout_width="150dp"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:background="@color/colorAccent"
        android:gravity="center"
        android:text="right"
        android:textColor="#ffffff" />

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="?android:colorBackground"
        android:foreground="?listChoiceBackgroundIndicator">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            tools:text="Content" />

    </FrameLayout>
</com.qdcares.qdcaresrecyclerview.swipemenu.SwipeItemLayout>